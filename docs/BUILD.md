# Build

See the main [README.md](../README.md) for:

- Host (desktop) build: `./scripts/build_host.sh`
- Android APK: `cd android && ./gradlew assembleDebug`
- Model download and quantization
- Deployment steps

For pipeline and component details, see [ARCHITECTURE.md](ARCHITECTURE.md).
